// Google Cloud Console → APIs & Services → Credentials → Create OAuth client ID
// → type "Web application" → add Authorized JavaScript origins for every URL
// this app is served from (e.g. http://localhost:8081, https://ff.gbogd.com).
// Paste the resulting client ID below. Leave empty to hide the Google button.
export const GOOGLE_CLIENT_ID = "";
